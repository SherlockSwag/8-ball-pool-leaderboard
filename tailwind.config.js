/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./public/**/*.html", // This will scan HTML files inside your public folder
    // Add other paths here if you have JS files, etc. that contain Tailwind classes
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}